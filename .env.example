# ---- Optional: Supabase (cloud sync)
# Create a Supabase project, then add your project URL + anon key here.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# ---- Optional: Eve server (OpenAI)
# If you run the local Node server for Eve, add your OpenAI key to server/.env instead.
